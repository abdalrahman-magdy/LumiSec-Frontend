# Backend API (no trailing slash)
REACT_APP_API_BASE_URL=http://localhost:3000

# Use real phishing API (not mock data)
REACT_APP_PHISHING_MOCK_FALLBACK=false

# Dev-only role switcher in Phishing header
REACT_APP_PHISHING_DEV_ROLE_SWITCHER=false

# React dev server — use 3001 because backend uses 3000
PORT=3001
